import { EntDirectoryInfo } from "../directoryInfo";

export class EntRestAccess {
  id: number = 0;
  restAlias: string = "";
  restName: string = "";
  restCategoryId: EntDirectoryInfo | null = null;
}
